# Lucy Self-Update Proposal

## User Request

add a hide command so I can tell Lucy to disappear and come back later

## Lucy's Proposal

### Self-Update Proposal for Lucy

#### 1. Goal
To update Lucy's scripts, files, and memory to ensure her continued functio[7D[K
functionality.

#### 2. Files Likely Changed
- `Lucy.swift`
- `README.md`
- `/memory/memory.json`

#### 3. Exact Behavior to Add/Change
- **Add a `hide` command**: This allows users to temporarily hide Lucy so s[1D[K
she doesn't interfere with their workspace until they want her back.
  
  ```swift
  // In `Lucy.swift`
  if let hideCommand = Command.arguments.last {
      guard hideCommand == "hide" else { continue }
      
      // Hide Lucy using Cocoa API
      let showWindow = false
      let hideWindow = true
      let windowStyle = NSApp.keyWindow?.windowStyle

      // Update the `showWindow` and `hideWindow` properties of the object [K
representing Lucy
      ...
  }

- **Save updated memory file**: Ensure that changes to scripts are properly[8D[K
properly saved after each update, so they can be easily reinstated.
  
#### 4. Risks
- The addition of a new command could potentially alter how Lucy interacts [K
with the environment or affect her behavior in unintended ways.
- There is a risk that hiding Lucy will cause users to accidentally remove [K
some of her functions.

#### 5. Manual Approval Needed
The user should have full control over adding this new command and test it [K
thoroughly before using it for the first time, ensuring no side effects occ[3D[K
occur.

#### 6. Test Plan
- Create a temporary folder `/test_lucy_updates` where Lucy will run withou[6D[K
without interacting with her environment.
- Inoculate Lucy's scripts and memory files against changes (e.g., by not c[1D[K
compiling them).
- Once the update is complete, observe how Lucy behaves under different con[3D[K
conditions (both hidden and unhidden).
  
By following these guidelines, we can ensure that our updates to Lucy are s[1D[K
safe, effective, and do not interfere with her normal operation.

## Safety Rule

This is only a proposal. Lucy has not edited code yet.

Future safe apply flow:
1. Backup the current file.
2. Create a patch.
3. Ask for user approval.
4. Apply only inside the Lucy project folder.
5. Compile with swiftc.
6. Roll back if compile fails.
